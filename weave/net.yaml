code: Redirect
message: >-
  Moved permanently to `/k8s/v1.10/net.yaml`, assuming Kubernetes version is
  "v1.10", old location (`/k8s/v1.13/net.yaml`) will be deprecated soon. If you
  are using `curl`, you can pass `--location`, but it is best to just use the
  new URL.
